# 메인 컨트롤 노드 → FMS: 신발 종류 하나(shoes_num)에 대한 배치 작업 지시.
# 비전 분류가 끝난 배치 결과를 FMS에 그대로 넘겨준다(FMS는 더 이상 비전 서비스를
# 직접 호출하지 않는다 — 메인 컨트롤 노드가 대신 이 서비스로 전달).
int32 shoes_num       # 신발 종류 인덱스 (0=A, 1=B, 2=C, 3=D)
int32[] shoes_length   # 배치에 포함된 각 신발의 길이(mm), 예: [280, 260, 240, 260, 280]
---
